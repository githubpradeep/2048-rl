env: pong
common:
  seed: 42
train:
  episodes: 3000
  eval-every: 50
  eval-episodes: 50
  double-dqn: true
  dueling: true
  save-dir: models/pong
eval:
  model: models/pong/pong_dqn_best.json
  episodes: 200

play:
  model: models/pong/pong_dqn_best.json
  mode: pygame
  delay: 0.06
